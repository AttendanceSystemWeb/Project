# Proxy API calls to backend FIRST (before catch-all)
# All requests to /api/* will be proxied to the backend
/api/*  https://site--sams-backend--zj5y5bs2dc2g.code.run/api/:splat  200

# Catch-all: serve index.html for all other routes (SPA routing)
/*    /index.html   200